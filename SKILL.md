---
name: project-management
description: Use when 用户要创建或接手长期受管项目、为旧项目制作独立标准化副本、开展项目级审查，或审查技能、规范、配置、提示词、文档系统的实际有效性时。纯解释、临时问答、拼写修正和不涉及项目整体管理的孤立单点小修，不自动触发完整项目流程。
---

# 项目管理

## 定位

本技能把项目目标、当前事实、工作计划、验证结果和历史变化保存在项目本地文件中，使人类与不同 Agent 能够基于同一份事实接力，而不是依赖聊天记录。

`SKILL.md` 与 `references/*.md` 共同构成**唯一正式行为规范**，使用简体中文；README 和其他人类文档只说明能力与使用方式，不得发展为第二套执行规则。详细模板和流程按需读取参考文件，不在本入口重复维护。

接手受管项目时，先读 `AGENTS.md`、`README.md` 和 `DEVLOG.md`，快速建立项目概览、定位关键文件并找到继续工作的起点。文档不是事实证明：涉及入口、依赖、配置、完成度、行为和验证状态的关键事实，必须查看实际代码、配置并运行适用命令核验。不得承诺仅凭文档即可“完全理解”项目。

## 何时不运行完整流程

下列请求通常直接处理，不创建事实层、不建立完整 DEVLOG 工作单元，也不强行套用三模式：

- 解释一个概念、命令、正则或代码片段；
- 孤立的错别字、格式或单点小修，且不涉及项目范围、架构、交接或项目级排查；
- 临时问答或一次性操作，用户没有长期项目管理意图。

若小请求发生在已受管项目内，仍先遵守该项目 `AGENTS.md` 的现行协议，但按有意义的工作单元控制留痕粒度，避免过度流程化。

## 模式选择

| 用户意图 | 模式 |
|---|---|
| 新建长期项目并建立本地事实层 | 模式一：创建受管项目 |
| 接手已经受本规范管理的项目继续工作 | 接力流程：先核验事实，再按现有工作单元实施 |
| 理解旧项目并在独立路径生成规范副本 | 模式二：标准化副本重构 |
| 查找并验证项目、技能、配置、规范或文档问题 | 模式三：证据化审查 |

意图不清或授权边界会影响安全时，先澄清再操作。一个请求可依次使用多个模式，但必须分别声明范围与完成条件；不得借模式切换绕过授权。

## 共通事实层与记录纪律

项目保持原生工具链和源码目录，不强制语言、框架、运行时、Git 或固定源码目录，也不强制创建 `技术/`。推荐事实层由以下文件按实际需要组成：

- `AGENTS.md`：工具中立的项目工作协议和 Agent 入口正本；
- `CLAUDE.md`：简短纯文本入口桩，只指向 `AGENTS.md`；
- `README.md`：可改写的当前项目事实地图；
- `DEVLOG.md`：计划、决策、结果、偏差和接力时间线；
- `CHANGELOG.md`：仅记录已经落地的用户可见变化；
- `requirements/INDEX.md`：存在输入材料时记录评估状态与吸收去向；
- `AUDIT.md`：受管项目首次执行模式三时创建的审查历史。

按需读取 `references/project-templates.md` 获取事实层、工作单元和材料索引模板；`AUDIT.md` 的轮次与发现结构在 `references/audit-checklist.md` 维护；涉及路径、编码、行尾、平台或安装时读取 `references/cross-platform.md`。

### DEVLOG

- 以有意义的工作单元为粒度，实质实施前记录目标、背景、计划、影响范围和预期验证；完成后追加实际结果、变更文件、验证、偏差、遗留项和状态。
- 历史条目正文只追加，不覆盖、不删除，也不回头改写旧计划来补状态；状态变化通过新的结果或补充小节追加。
- 顶部 `当前状态` 是通常唯一可改写区域，用于维护当前阶段、最近完成、进行中、下一步、阻塞项和关键验证命令。
- 不改变语义的拼写修正可直接完成；敏感信息误入时允许脱敏，但须追加说明，并对暴露凭据采取轮换等实际处置。
- 一个工作单元可对应零个、一个或多个 Git commit；不默认提交，也不要求一条记录恰好对应一次提交。

### CHANGELOG

只在完整功能、修复或移除已经落地后记录用户可见变化。计划、调查过程、内部整理、未完成工作和仅有审查结论不进入 CHANGELOG。

### 材料

新材料通过 `requirements/INDEX.md` 记录“待评估、已评估、部分采纳、暂缓、拒绝”等状态及吸收位置。原始材料默认不重命名、不修改、不删除；新流程不以添加 `[已读]` 前缀表示处理完成。已有 `需求/` 和带 `[已读]` 前缀的材料继续作为历史兼容信号识别，不强制迁移或改名。

## 接力流程

1. 阅读并遵守 `AGENTS.md`。
2. 阅读 `README.md`，再读 `DEVLOG.md` 的 `当前状态` 和末尾相关工作单元。
3. 如有 `requirements/INDEX.md`，检查待评估或部分采纳材料；兼容识别既有 `需求/` 与 `[已读]`。
4. 查看实际代码、配置、依赖清单和适用命令，核验本轮依赖的关键事实。
5. 先建立或续接 DEVLOG 工作单元，再实施实质改动。
6. 运行针对性验证并如实区分通过、失败、未执行、环境受限和结果不确定。
7. 追加结果、更新 `当前状态`；只有已落地的用户可见变化才写入 CHANGELOG。

## 模式一：创建受管项目

目标是在不破坏原生工具链的前提下，先建立可信事实层，再进行实质搭建。

1. 确认项目位置；用户已明确给出时不重复询问，未明确时先问。
2. 理解项目目的、用户、范围、非目标和成功标准；关键歧义先澄清。
3. 确定项目原生工具链与目录布局，保留其自然结构。
4. 按需读取 `references/project-templates.md`，建立最小且有内容的事实层；无输入材料时不机械创建空 `requirements/`，不机械保留空章节。
5. 有材料时建立 `requirements/INDEX.md` 并评估，保持原材料不变。
6. 在 README 写当前项目地图，在 DEVLOG 建立首个真实工作单元。
7. **先完成事实层，再开始实质实施。** 实施后执行验证并回写记录。

Git 推荐但可选，不是使用本技能的前提。是否初始化或使用本地 Git 依据用户请求、现有上下文和授权决定；不得因没有 Git 阻塞项目，也不得擅自初始化、提交或把远程操作当成默认步骤。

## 模式二：标准化副本重构

模式二只读理解源项目，并在**独立目标路径**创建可运行、可验证、可接力的标准化副本。执行前必须读取 `references/migration-copy-mode.md`。

硬性边界：

- 源项目全程只读；严禁写入、移动、删除、重命名、覆盖或删减源项目内容。
- 严禁把源目录本身当作目标，也不得以任何方式在源项目上直接完成迁移。
- 源路径与目标路径必须不同；目标不得位于源目录内，源目录不得位于目标内；解析符号链接后仍须满足隔离关系。
- 非空目标默认停止，只有用户明确批准具体合并或覆盖策略后才能继续。

执行顺序：

1. 确认源目录、目标目录、新项目名称、Git 历史策略、大型资产策略和敏感文件策略。
2. 只读分析源项目，以实际代码、配置、依赖、文档和可运行命令建立理解。
3. 制定复制方案，将内容分为直接复制、目标中重新生成、选择性复制、默认排除和敏感内容。
4. 在独立目标路径创建标准化副本，保留必要原生内容与工具链布局，并建立事实层。
5. 验证副本的关键文件、启动/构建/测试命令、绝对路径残留和敏感内容排除情况。
6. 用操作前后文件清单、大小、内容哈希及适用时的 Git 状态证明源项目未变；修改时间只能作为辅助证据。
7. 交付复制范围、排除项、验证结果、限制和源项目未改动证据。

凭据、令牌、私钥、真实 `.env` 值和个人隐私数据默认排除。依赖目录、缓存、构建产物和临时文件是候选排除项，最终分类必须服从项目事实，不得误删不可替代输入或交付物。

## 模式三：证据化审查

模式三审查代码、配置、数据流程、技能、提示词、规范或文档系统。执行前必须读取 `references/audit-checklist.md`。

核心原则：产出是经过证据验证的问题，不是看起来全面的报告；宁可少报，不可虚报。

1. **建立审查契约**：明确目标、对象与范围、范围外内容、是否允许修改、修改授权边界、验证方式和完成标准。用户说“只检查”“只报告”或“不要修改”时，全程只读，任何严重级别都不得修改。
2. **建立覆盖底账**：记录已盘点、已检查、未检查、排除项及理由、核心链路、抽样内容和无法读取或运行的部分。部分覆盖不得声称全面审查。
3. **选择独立标准**：以需求、测试、协议、平台规范、运行环境、目标读者需求和真实行为判断；不得用被审查对象自己的声明作为正确性的唯一证明。
4. **验证候选问题**：给出具体失败场景，即“输入或状态 → 执行路径 → 错误行为 → 影响”，并通过追踪完整路径、构造输入或实际运行取得证据。
5. **主动反证**：检查上游校验、调用约束、框架保证、配置前提、其他实现分支和业务目标。合理反证未排除时，不得标为已确认。
6. **分离两条轴**：确定性分为“已确认、疑似待验证、建议”；已确认缺陷的严重性另分为“致命、严重、一般、轻微”。建议使用独立优先级，不得包装成缺陷。
7. **记录最低证据**：确认问题至少包含位置、触发条件、实际行为、预期行为、证据、影响、处理、验证，以及发生修复时的关联 DEVLOG ID。
8. **审查与修复分离**：发现 → 验证 → 记录结论 → 检查授权 → 建立 DEVLOG 工作单元 → 实施修复 → 回归验证 → 回写 AUDIT 处理结果。没有授权就停在记录结论。

技能、规范、提示词、配置和文档等非代码对象，除静态阅读外，还必须用正常、模糊、压力、冲突、边界和不应触发场景验证实际效果。逐行通读不能单独证明有效。

本技能不依赖任何外部技能、插件或工作流。`SKILL.md` 与 `references/*.md` 自足；缺少其他工具不构成阻塞，也不得引入对它们的隐性依赖。

## 安全与授权边界

- 敏感值默认不复制、不写入公共文档、不输出到日志；发现疑似秘密时先停止扩散并说明处置需求。
- 删除、覆盖、批量重命名、不可逆迁移及其他难以恢复的操作，必须取得用户对具体范围的明确授权。
- 创建远程仓库、push、远程分支变更、PR、Release 和外部发布，必须取得用户明确授权；本地 Git 授权不等于远程授权。
- 只读授权下不得修改文件、配置、历史、元数据或审查对象；严重性不构成越权理由。
- 不使用作者机器绝对路径；示例和记录使用 `<project-root>`、`<source-project>`、`<target-project>` 等占位路径。
- 核心行为不依赖 Python、Node.js、Bash、PowerShell、Git、GitHub CLI、rsync、robocopy 或符号链接；可按环境选择工具，但不能把推荐工具变成硬依赖。
- 不把未执行、失败、受限或不确定的验证描述为通过，也不把兼容性设计描述为真实平台实测。
- 不维护中英文两套执行规范；若说明文档与本技能及 references 冲突，以简体中文行为规范正本为准并报告冲突。

## 完成条件

任何模式完成时都必须能够报告：

- 实际范围与未覆盖内容；
- 创建或修改的内容，以及明确未执行的操作；
- 关键事实的核验依据；
- 验证命令或方法及其真实状态；
- 遗留问题、限制和下一步；
- 授权边界是否得到遵守；
- DEVLOG、AUDIT、CHANGELOG 等适用记录是否按职责更新。

模式一还须确认事实层先于实质实施且原生结构未被强制改造；模式二还须提供独立目标副本和源项目未变证据；模式三还须提供审查契约、覆盖底账、具体失败场景、反证结果，并保持确定性、严重性、建议及修复授权相互分离。
